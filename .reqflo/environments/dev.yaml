environment:
  key: dev
  label: Development

runDefaults:
  baseUrl: https://api.dev.example.com
  timeoutMs: 5000
  retries: 1
  headers:
    x-reqflo-test-suite: smoke

testData:
  tenantId: demo-dev
  userSegment: beta
  currency: USD