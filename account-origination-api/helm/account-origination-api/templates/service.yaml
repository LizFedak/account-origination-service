apiVersion: v1
kind: Service
metadata:
  name: {{ include "account-origination-api.fullname" . }}
  labels:
    {{- include "account-origination-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "account-origination-api.selectorLabels" . | nindent 4 }}
